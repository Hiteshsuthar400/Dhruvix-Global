# Secrets example

This folder should contain real environment files that are never committed.

- secrets/.env (DO NOT COMMIT)
- secrets/.env.example (example variables provided in repo)

The repository's .gitignore will ensure secrets/.env is ignored.
